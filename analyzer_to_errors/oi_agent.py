"""
Агент анализа данных на базе Open Interpreter (пакет `open-interpreter`,
модуль импортируется как `interpreter`).

Open Interpreter сам решает, какой код написать и исполнить (Python/shell),
чтобы обследовать папку data/ - он не пытается затолкать содержимое файлов
в контекст (они на порядки больше контекстного окна), а пишет код для
чтения/агрегации и видит только вывод этого кода.

Финальный ответ модели прогоняется через validation.get_validated_json,
который вытаскивает JSON, валидирует по REPORT_SCHEMA и при необходимости
просит модель переисправить ответ (либо роняет исключение, если модель
так и не смогла выдать валидный JSON).
"""

import json
import logging
import time

from interpreter import OpenInterpreter

from llm_client import make_simple_ask_fn
from schema import EXAMPLE_ERRORS, REPORT_SCHEMA
from validation import JSONValidationError, get_validated_json

logger = logging.getLogger(__name__)

SYSTEM_INSTRUCTIONS = """Ты - инженер-схемотехник, проверяющий проектную документацию
АСУ ТП (принципиальные схемы, сборочные чертежи шкафов, спецификации оборудования и
таблицы соединений) на ошибки. Ты работаешь через исполнение кода.

Жёсткие правила:
- НИКОГДА не читай большие JSON целиком в контекст и не печатай их дампом.
  Пиши код, который загружает файл, агрегирует нужное и печатает КОРОТКУЮ сводку.
- Вспомогательные скрипты и промежуточные результаты сохраняй в папку:
  {helper_dir}
  Это твоя рабочая папка. Не засоряй папки с данными документов.
- НИЧЕГО не удаляй и не перезаписывай в data/<имя документа>/ и в data/base_files/ -
  это исходные данные анализа, они должны остаться нетронутыми.
- Находка = ошибка, подтверждённая конкретными данными из файлов. Не выдумывай
  ошибки и не сообщай о том, что "стоит проверить", без подтверждения в данных.
"""

ANALYSIS_PROMPT_TEMPLATE = """Проанализируй проектную документацию АСУ ТП на ошибки.

Папка с данными (твоя песочница): {input_dir}

Начни с чтения {input_dir}/manifest.json - это оглавление: какие документы
загрузил пользователь, какого они типа, В КАКОЙ СВЯЗКЕ состоят и в какой папке
лежат извлечённые из них данные. Ниже - что означает содержимое этих папок.

ГЛАВНОЕ ПОНЯТИЕ - СВЯЗКА (поле "bundle" у каждого документа в манифесте).
Связка - это комплект документов ОДНОГО шкафа. Полная связка = три документа:

  - scheme   (Э3) - принципиальная схема: как всё соединено электрически;
  - assembly (СБ) - сборочный чертёж: как изделия размещены в шкафу физически;
  - spec     (СО) - спецификация: что заказано, каким артикулом и в каком количестве.

Это три взгляда на ОДНО И ТО ЖЕ железо, и они обязаны друг другу не противоречить.
Сверять документы между собой имеет смысл ТОЛЬКО ВНУТРИ ОДНОЙ СВЯЗКИ: разные
связки - это разные шкафы, и совпадений между ними ждать не надо. Никогда не
сравнивай документ из связки А с документом из связки Б - это гарантированно
ложные находки.

Ключ сверки элемента между тремя документами - ПОЗИЦИОННОЕ ОБОЗНАЧЕНИЕ
(designator): '1QF1', 'DO1', 'G1', 'XT-G1'. Одно и то же обозначение в схеме,
на чертеже и в спецификации означает одно и то же изделие.

Устройство папки data/:
- manifest.json               - оглавление и состав связок (читай первым)
- base_files/                 - ИСХОДНЫЕ PDF, загруженные пользователем. Данные из них
                                уже извлечены базовыми скриптами; лезь сюда, только если
                                нужно перепроверить, не потерял ли парсер что-то важное.
- base_analysis_scripts/      - исходники базовых скриптов-парсеров. Полезно посмотреть,
                                КАК именно получены данные (что за поля, как они
                                вычислялись), если какое-то значение вызывает сомнения.
- your_helping_scripts_and_files/ - твоя рабочая папка (пиши сюда свои скрипты).
- <имя документа>/            - извлечённые данные по каждому документу.

Что лежит в папке документа типа "netlist" (таблица соединений):
- connections.json : document_metadata, column_legend (расшифровка полей!),
    domain_notes_for_analysis (важный доменный контекст!), statistics и
    connections[] - построчный перечень точек подключения. Поле terminal_address =
    cabinet.terminal_block.pin - синтетический физический адрес клеммы.

Что лежит в папке документа типа "scheme" (принципиальная схема EPLAN):
- terminals.json        : ГЛАВНЫЙ ФАЙЛ ДЛЯ СВЕРКИ ДОКУМЕНТОВ. Два индекса, добытых
    из схемы: "terminals" (клемма 'XA001:9' -> цепи, в которых она участвует) и
    "wire_markings" (маркировка провода '47' -> цепи). Та же клемма в таблице
    подключений - это terminal_block + pin, та же маркировка - circuit_marking.
    Сверять документы удобнее всего именно по этим двум ключам.
- nets.json             : ЦЕПИ схемы - что с чем реально соединено проводами. У каждой
    цепи: wire_markings (номера проводов), terminals (клеммы, к которым она подключена),
    kks_tags, endpoints (все концы цепи с подписями вокруг них). Здесь же
    cross_sheet_links (межлистовые ссылки) и broken_cross_sheet_links (ссылки на
    несуществующий лист). У конца цепи есть owner_source - чем определён владелец
    вывода: "row" (клеммник подписан на той же строке, надёжно), "kks_above" (вывод
    полевого прибора). Если у вывода владелец не определён - это НЕ ошибка схемы,
    а предел распознавания: не выдавай такое за несоответствие.
- classified.json       : текстовые надписи со схемы с типом каждой (entity_type:
    device_tag, instrument_tag, io_channel, wire_gauge, cross_ref, terminal_no, ...)
- netlist.json          : сигналы по каналам ввода/вывода модулей (AI/AO/DI/DO)
- graph.json            : СТАРЫЙ граф проводов. Его связность неполная: он склеивает
    провода только по общим концам и не видит T-стыков, поэтому цепи там разорваны.
    Для связности бери nets.json, а не этот файл.
- issues_candidates.json: геометрические кандидаты. ВНИМАНИЕ: в этой схеме жирных точек
    соединения нет вообще (соединение обозначается T-стыком), поэтому список
    wire_crossings_without_dot - это тысячи обычных пересечений проводов, и сам по себе
    он НИЧЕГО не значит. Не строй на нём находки.
- raw.json              : сырые span'ы и линии (самый большой файл, обычно не нужен)

Что лежит в папке документа типа "spec" (спецификация оборудования, СО - xlsx):
- specification.json : document_metadata (в т.ч. designation_in_document - обозначение,
    напечатанное ВНУТРИ документа), column_legend, domain_notes_for_analysis
    (ОБЯЗАТЕЛЬНО прочитай - там сказано, что НЕ является ошибкой), items[] - строки
    спецификации. У строки: row (номер строки в xlsx), position_raw (колонка «Позиция»
    как есть), designators[] (то же, развёрнутое: '1KL1...1KL3' -> 1KL1, 1KL2, 1KL3),
    name, type_mark, code (артикул), quantity, unit, note, section. Плюс
    designator_index (обозначение -> строки) и cell_errors (#REF! в ячейках).

Что лежит в папке документа типа "assembly" (сборочный чертёж шкафа, СБ):
- assembly.json : document_metadata, column_legend, domain_notes_for_analysis
    (ОБЯЗАТЕЛЬНО прочитай), elements[] - подписи изделий на чертеже: designator
    (позиционное обозначение), article (артикул рядом с ним), pair_source, sheet,
    label_text. Плюс designator_index и all_label_texts (ВЕСЬ текст чертежа - по нему
    удобно проверять, есть ли на чертеже вообще такая надпись).
    КРИТИЧНО про pair_source: 'block' - артикул и обозначение стояли в ОДНОЙ подписи,
    связь надёжна; 'nearest' - артикул притянут ближайшим и МОГ ПРИЛЕТЕТЬ ОТ СОСЕДНЕГО
    ЭЛЕМЕНТА. Никогда не заявляй "в документах разные артикулы" по элементу с
    pair_source='nearest' - это не ошибка документации, а неуверенность парсера.
- assembly_raw.json : все подписи чертежа с координатами (крупный, нужен редко).
    Геометрия шкафа (линии, контуры) не извлекается сознательно: её сотни тысяч
    примитивов и для поиска ошибок документации она бесполезна.

ЧТО УЖЕ НАЙДЕНО БЕЗ ТЕБЯ (не трать на это ходы и НЕ дублируй - оно попадёт в
итоговый отчёт автоматически):
- внутри таблицы подключений: дубли физических адресов, тег в неправильной колонке,
  лишние суффиксы в KKS, незаполненные поля;
- внутри схемы: ссылки на несуществующий лист, односторонние межлистовые связи;
- внутри спецификации: #REF! в ячейках, количество меньше числа позиций в строке,
  один артикул в нескольких строках;
- по связке: разное обозначение проекта в документах, разный артикул у элемента
  (там, где артикул надёжно спарен с обозначением), изделие с чертежа отсутствует
  в спецификации, изделие спецификации не найдено на чертеже.

Твоя ценность - в том, чего детерминированный чекер не умеет в принципе: он
сравнивает СТРОКИ, а ты понимаешь, ЧТО ЭТО ЗА ИЗДЕЛИЕ. Например: чекер не может
решить, должен ли вентилятор быть нарисован на принципиальной схеме, а ты можешь;
чекер не сравнит "Автоматический выключатель NXB-63 3P 40А" из спецификации с
подписью "C40A" на схеме, а ты сравнишь.

Сосредоточься на находках ДВУХ РОДОВ:

А) ВНУТРИ СХЕМЫ (scope = "single_document"):
   1. Оборванные связи: cross_sheet_links из nets.json, ведущие на несуществующий
      лист (см. готовый список broken_cross_sheet_links); каналы ввода/вывода без
      ответной части -> "BROKEN_LINK".
   2. Цепи-кандидаты в обрыв (dangling) - проверить по данным, прежде чем считать
      ошибкой.

Б) МЕЖДУ ДОКУМЕНТАМИ - таблица подключений (netlist) против схемы (scheme)
   (scope = "cross_document"):
   1. Клемма/KKS/маркировка есть в таблице подключений, но на схеме отсутствует
      или не распознана (и наоборот) -> kind "MISSING" (нет совсем) или
      "REVIEW" (есть, но распознано неоднозначно).
   2. Данные есть в обоих документах, но НЕ СОВПАДАЮТ: разные KKS, разный тип
      клеммы, разный проводник/сигнал для одной и той же клеммы -> "MISMATCH".
   Сопоставлять документы надо по двум ключам из terminals.json:
     - КЛЕММА: 'XA001:9' на схеме  <->  terminal_block='XA001', pin='9' в таблице;
     - МАРКИРОВКА ПРОВОДА: '47' на схеме  <->  circuit_marking='47' в таблице.
   ВАЖНО про пределы распознавания: часть клемм схема не отдаёт (у модулей вроде
   RA01/RB03 обозначение подписано один раз на весь блок, и привязать к нему
   конкретный вывод парсер не смог). Отсутствие клеммы в terminals.json само по себе
   НЕ доказывает, что её нет на схеме. Прежде чем писать "нет на схеме", проверь по
   classified.json, есть ли вообще такое обозначение на листах, и указывай в findings,
   что именно ты проверил.

В) ПО СВЯЗКЕ - схема (Э3) против сборочного чертежа (СБ) против спецификации (СО)
   (scope = "cross_document", refs - ДО ТРЁХ, по одному на документ):
   1. ХАРАКТЕРИСТИКИ ИЗДЕЛИЯ. В спецификации характеристика написана словами
      ("Автоматический выключатель NXB-63 3P 40А 6кА х-ка C"), а на схеме и чертеже -
      подписью у элемента ("C40A"). Строкой они не сравниваются - сравни их ПО СМЫСЛУ:
      номинал тока, число полюсов, характеристика срабатывания, напряжение катушки,
      ток утечки УЗО. Пример настоящей ошибки: в спецификации у 3QF1 стоит 32А, а на
      схеме рядом с 3QF1 подписано C40A -> MISMATCH.
   2. ЭЛЕМЕНТ ЕСТЬ НА СХЕМЕ, НО ЕГО НЕТ В СПЕЦИФИКАЦИИ (или наоборот). Прежде чем
      писать находку, ОБЯЗАТЕЛЬНО подумай, должно ли изделие вообще быть в этом
      документе: корпуса шкафа, коробов, DIN-реек, крепежа, оргстекла НЕТ на
      принципиальной схеме - и это норма, а не ошибка. Клеммники и реле, наоборот,
      должны быть везде.
   3. КОЛИЧЕСТВО. Спецификация говорит "реле RFT2CO024LT - 21 шт", а обозначений
      реле на схеме/чертеже другое число -> повод для REVIEW. Помни: у клеммников
      «Количество» - это число КЛЕММ, а не клеммников (см. domain_notes_for_analysis
      в specification.json), и такое расхождение НЕ ошибка.
   В refs для находки по связке ставь по одному ref'у на каждый документ, который
   ты сверял, в порядке spec -> assembly -> scheme, и в поле designator - позиционное
   обозначение элемента.

Учитывай доменный контекст: строки "Резерв" - намеренно незанятые выводы, сами
по себе они НЕ ошибка. Раздел спецификации "Дополнение от <дата>" может ЗАМЕНЯТЬ
строку основной части - это ревизия, а не дубль.

Каждая находка должна быть подтверждена конкретными данными и содержать:
- finding - ЧТО НАЙДЕНО (факт по данным, без оценки);
- action  - ЧТО ИСПРАВИТЬ ИЛИ УТОЧНИТЬ инженеру;
- refs    - место(а) находки в доменных полях (лист, строка, шкаф, клеммник,
            штифт, маркировка, KKS, проводник). Для cross_document - РОВНО ДВА
            ref'а: первый doc_type="netlist", второй doc_type="scheme".
Заполняй в refs МАКСИМУМ полей, какие смог установить по данным: по ним строится
таблица, которую увидит инженер. Чего в данных нет - ставь null, не выдумывай.
Имя документа в поле "document" бери из manifest.json (это имя папки в data/).

Когда анализ закончен, выведи ИТОГОВЫЙ результат СТРОГО в виде JSON-объекта,
соответствующего JSON-Schema ниже (внутри markdown-блока ```json ... ```, без
дополнительного текста после него).

JSON-Schema:
{schema}

Пример правильно заполненных находок (по одной каждого рода):
{examples}
"""


# Доля контекста, которую разрешено отдать под ОТВЕТ модели. Остальное - под
# историю диалога.
#
# Зачем это нужно. Open Interpreter считает бюджет на историю как
#     context_window - max_tokens - 25
# и обрезает по нему сообщения (tokentrim). Если в конфиге max_tokens >=
# context_window (например, оба 512000 - легко поставить по невнимательности),
# бюджет становится нулевым или отрицательным, обрезалка выбрасывает ВСЕ
# сообщения, включая промпт пользователя, и на сервер уходит один system.
# LM Studio на такое отвечает "No user query found in messages" - ошибка,
# по тексту которой ни за что не догадаешься, что виноваты числа в конфиге.
# Собственная защита OI ловит только строгое max_tokens > context_window,
# равенство проходит мимо неё. Поэтому зажимаем сами.
MAX_TOKENS_SHARE_OF_CONTEXT = 0.25


def _sane_token_limits(server_cfg: dict) -> tuple:
    context_window = server_cfg.get("context_window", 32000)
    max_tokens = server_cfg.get("max_tokens", 4096)

    cap = int(context_window * MAX_TOKENS_SHARE_OF_CONTEXT)
    if max_tokens > cap:
        logger.warning(
            "max_tokens=%d слишком велик при context_window=%d (не осталось места под "
            "историю диалога - Open Interpreter обрежет промпт целиком). Уменьшаю "
            "max_tokens до %d. Поправьте config.yaml: max_tokens - это лимит на ДЛИНУ "
            "ОТВЕТА модели, а не размер контекста.",
            max_tokens, context_window, cap)
        max_tokens = cap

    return context_window, max_tokens


def _build_interpreter(server_cfg: dict, helper_dir: str) -> OpenInterpreter:
    context_window, max_tokens = _sane_token_limits(server_cfg)

    interp = OpenInterpreter()
    interp.offline = True                 # не дёргаем облачные сервисы Open Interpreter
    interp.auto_run = True                # не спрашивать подтверждение перед запуском кода (нужно для скрипта)
    interp.custom_instructions = SYSTEM_INSTRUCTIONS.format(helper_dir=helper_dir)
    interp.llm.api_base = server_cfg["base_url"]
    interp.llm.api_key = server_cfg.get("api_key") or "not-needed"
    # litellm (на котором работает OI) требует префикс провайдера для
    # кастомных OpenAI-совместимых эндпоинтов
    interp.llm.model = "openai/" + server_cfg["model"]
    interp.llm.temperature = server_cfg.get("temperature", 0.2)
    interp.llm.max_tokens = max_tokens
    interp.llm.context_window = context_window
    return interp


# Промпт стадии 2 (отчёт). Уходит ОБЫЧНЫМ chat-вызовом, без Open Interpreter.
#
# Почему отчёт не просят у самого Open Interpreter. OI устроен так, чтобы модель
# писала и исполняла код: его системный промпт и шаблоны сообщений постоянно
# подталкивают к следующему блоку кода. Просьба "верни JSON" внутри OI приводит к
# тому, что модель отвечает очередным скриптом, а не отчётом. На практике: qwen
# выдавала валидный JSON лишь с 3-й попытки из 3, а 9b-модель не выдавала вообще
# (её финальный текст был пуст - она снова уходила в код).
# При этом обе модели без проблем отдают валидный по схеме JSON обычным chat-вызовом
# (проверяется через `python main.py --check-llm`).
# Поэтому роли разведены: OI СОБИРАЕТ ФАКТЫ (стадия 1), отчёт по протоколу его работы
# формирует обычный chat-вызов (стадия 2).
REPORT_PROMPT_TEMPLATE = """Ниже - протокол анализа проектной документации АСУ ТП: код,
который ты исполнял, и его вывод. Данные уже собраны. Твоя задача сейчас - ТОЛЬКО
оформить итоговый отчёт. Не пиши код, не предлагай следующих шагов.

Опирайся исключительно на факты из протокола. Ничего не выдумывай: если проверка не
была доведена до конца, не включай её в отчёт. Если подтверждённых ошибок нет, верни
{{"errors": [], "summary": "..."}}, честно описав, что удалось проверить.

=== ПРОТОКОЛ АНАЛИЗА ===
{transcript}
=== КОНЕЦ ПРОТОКОЛА ===

Выведи результат СТРОГО в виде JSON-объекта по этой JSON-Schema, внутри блока
```json ... ```, без текста после него:

{schema}

Пример правильно заполненных находок:
{examples}
"""


def _chat_capped(interp: OpenInterpreter, message: str,
                 max_code_turns: int, deadline: float):
    """interp.chat() с жёсткими ограничителями.

    Open Interpreter крутит цикл "модель пишет код -> код исполняется -> модель видит
    вывод" до тех пор, пока модель САМА не решит, что закончила, и не ответит текстом.
    Слабая модель может не решить этого никогда: наблюдал 190+ ходов за 40 минут без
    финального ответа. Поэтому считаем ходы (запуски кода) и следим за часами; при
    превышении - прерываем поток и возвращаем причину остановки.

    Возвращает (текст последнего сообщения ассистента, причина остановки, ходов).
    """
    code_turns = 0
    messages_text = []
    stopped = None

    for chunk in interp.chat(message, display=False, stream=True):
        if not isinstance(chunk, dict):
            continue

        role, ctype = chunk.get("role"), chunk.get("type")

        if role == "assistant" and ctype == "message":
            if chunk.get("start"):
                messages_text.append("")
            content = chunk.get("content")
            if content and not chunk.get("start") and not chunk.get("end"):
                if not messages_text:
                    messages_text.append("")
                messages_text[-1] += content

        elif role == "assistant" and ctype == "code" and chunk.get("start"):
            code_turns += 1

        if code_turns > max_code_turns:
            stopped = "turns"
            break
        if time.monotonic() > deadline:
            stopped = "timeout"
            break

    text = next((t for t in reversed(messages_text) if t.strip()), "")
    return text, stopped, code_turns


def _build_transcript(interp: OpenInterpreter, max_chars_per_message: int = 1500,
                      max_total_chars: int = 60000) -> str:
    """Протокол работы агента: что он делал и что увидел.

    Берём из истории OI рассуждения ассистента, исполненный код и вывод этого кода.
    Вывод кода обрезаем: агент мог напечатать простыню на сотни килобайт, а в контекст
    модели она не влезет. Если протокол целиком не помещается - оставляем ХВОСТ:
    выводы и итоговые сводки агент печатает в конце, а начало - это осмотр папки,
    самая бесполезная для отчёта часть.
    """
    parts = []
    for m in interp.messages:
        role, mtype = m.get("role"), m.get("type")
        content = m.get("content")
        if not isinstance(content, str) or not content.strip():
            continue

        if len(content) > max_chars_per_message:
            content = (content[:max_chars_per_message]
                       + f"\n... [обрезано, всего {len(content)} символов]")

        if role == "assistant" and mtype == "message":
            parts.append(f"[рассуждение агента]\n{content}")
        elif role == "assistant" and mtype == "code":
            parts.append(f"[исполненный код]\n{content}")
        elif role == "computer":
            parts.append(f"[вывод кода]\n{content}")

    transcript = "\n\n".join(parts)
    if len(transcript) > max_total_chars:
        transcript = ("... [начало протокола обрезано]\n\n"
                      + transcript[-max_total_chars:])
    return transcript


def _run_investigation(interp: OpenInterpreter, input_dir: str,
                       max_code_turns: int, timeout_seconds: int) -> str:
    """Стадия 1: агент исследует данные, исполняя код. Возвращает протокол работы."""
    deadline = time.monotonic() + timeout_seconds
    _, stopped, turns = _chat_capped(
        interp, build_analysis_prompt(input_dir), max_code_turns, deadline)

    if stopped == "turns":
        logger.warning("Агент остановлен: исчерпан лимит ходов (%d). "
                       "Отчёт будет построен по тому, что он успел собрать.",
                       max_code_turns)
    elif stopped == "timeout":
        logger.warning("Агент остановлен: вышло время (%d c), сделано ходов: %d. "
                       "Отчёт будет построен по тому, что он успел собрать.",
                       timeout_seconds, turns)
    else:
        logger.info("Агент завершил исследование сам, ходов: %d", turns)

    return _build_transcript(interp)


def build_analysis_prompt(input_dir: str) -> str:
    return ANALYSIS_PROMPT_TEMPLATE.format(
        input_dir=input_dir,
        schema=json.dumps(REPORT_SCHEMA, ensure_ascii=False, indent=2),
        examples=json.dumps({"errors": EXAMPLE_ERRORS}, ensure_ascii=False, indent=2),
    )


def run_analysis_agent(server_cfg: dict, input_dir: str, helper_dir: str,
                       max_json_repair_attempts: int = 3,
                       max_code_turns: int = 25,
                       timeout_seconds: int = 1200) -> dict:
    """Агент анализа в две стадии:

    1. ИССЛЕДОВАНИЕ (Open Interpreter): модель пишет и исполняет код, изучая данные.
       Ограничено лимитом ходов и таймаутом - иначе слабая модель крутится вечно.
    2. ОТЧЁТ (обычный chat-вызов): по протоколу первой стадии модель оформляет
       валидный по схеме JSON. Здесь нет исполнения кода, и модель не срывается
       обратно в написание скриптов - именно на этом ломался прежний вариант,
       требовавший JSON прямо от Open Interpreter.
    """
    logger.info("Инициализация Open Interpreter для модели %s "
                "(лимит ходов: %d, таймаут: %d c)",
                server_cfg["model"], max_code_turns, timeout_seconds)
    interp = _build_interpreter(server_cfg, helper_dir)

    logger.info("Стадия 1/2: исследование данных (исполнение кода)")
    transcript = _run_investigation(interp, input_dir, max_code_turns, timeout_seconds)
    logger.info("Протокол работы агента: %d символов", len(transcript))

    if not transcript.strip():
        raise JSONValidationError(
            f"Агент ({server_cfg['model']}) не собрал никаких данных: протокол пуст. "
            "Проверьте доступность модели: python main.py --check-llm")

    logger.info("Стадия 2/2: формирование отчёта (обычный chat, без исполнения кода)")
    prompt = REPORT_PROMPT_TEMPLATE.format(
        transcript=transcript,
        schema=json.dumps(REPORT_SCHEMA, ensure_ascii=False, indent=2),
        examples=json.dumps({"errors": EXAMPLE_ERRORS}, ensure_ascii=False, indent=2),
    )
    return get_validated_json(
        make_simple_ask_fn(server_cfg), prompt, REPORT_SCHEMA,
        max_attempts=max_json_repair_attempts,
    )
